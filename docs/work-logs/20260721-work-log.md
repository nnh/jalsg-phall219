# 2026-07-21 作業ログ

## 概要

解析パイプラインの計画づくりと、その汎用文書の適切な置き場の整理。当初 phall219 リポに作成した汎用メソッド文書2件を、試験横断の方法論と判断して akiko-office へ移設した。あわせて旧 stat-hub 廃止に伴う陳腐化参照を修正。

## 決定事項

- **納品はRベース**（研究者はSAS非所有・Rなら追加解析可）。SAS・R二重コーディングで解析し（明子先生はSAS主軸）、**compareは内部検証専用でPI非提示**。compareの粒度はTLFでなく**CDISC ARSのARDレベル**（`waldo::compare()`）。
- **モデル配分**：前半（ARS設計・ADaM・中核エンドポイント導出）はFable、後半（ARD生成・図表描画）はSonnet の2ブロックに集約しモデル切替を原則1回に。人の介入は必須2点（データロック・納品承認）＋条件付き2点（新規解釈・compare不一致）。指示不足時は着手しないプリフライトゲートを規定。
- **連番は1始まり（Phase 0を作らない）** をグローバル規約に明示（ai-environment `20260619-living-doc-structure.md`）。
- **リポジトリ組織配置の3層原則**：機関成果物＝`nnh`／個人の方法論＝`akikomsaito`／家族＝`saito-la`。旧 `saito-la/stat-hub` は2026-07-20廃止・`akiko-office` に統合済み。

## 本リポジトリでの変更

- `CLAUDE.md`：`saito-la/stat-hub`（廃止済み）参照を `akikomsaito/akiko-office` に更新。
- `docs/analysis-pipeline-plan.md`・`docs/cdisc-ars.md`：本セッションで作成後、試験横断の汎用メソッドのため **akiko-office（`docs/methods/`）へ移設**（コミット `7226c19`）。本リポには残さない。
- `next-action.md`：明子先生が挙げた「配置意図の確認」を解決済みに更新（移設セッションが実施）。
- `docs/work-logs/20260721-work-log.md`（本ファイル）。
- `~/Projects/stat/CLAUDE.md`（コンテナ・git非管理）：ハブ参照を akiko-office に更新（ディスク保存のみ）。

## 連絡

- 移設完了を明子先生へChat通知（2026-07-21 07:16 JST）。明子先生は事前に配置に同意済み（「共通の場所が理想だが、無いのでそこで良い。後で移せる」）。

## 次回アクション

- next-action.md のとおり（データロック待ち → saihi.csv 再導出 → CSVtoSASDS.sas のPhALL219改修 →）。
- 汎用メソッドの以後の更新は akiko-office 側で行う。
